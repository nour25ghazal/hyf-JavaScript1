let mixarr = [1, 2, 3, " hello ", true];
console.log("we can store multiple types in an array " + mixarr);
let x = 6 / 0;
let y = 10 / 0;
console.log(x);
console.log(y);
if (x == y) {
    console.log("we can compare infinities")
} else {
    console.log("we can't compare infinities")
}